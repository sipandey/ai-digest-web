# AI Digest — Web

This is the Next.js frontend for AI Digest. See the [root README](../README.md) for full setup instructions, architecture documentation, and the deployment checklist.

## Quick start

```bash
npm install
cp ../.env.example .env.local
# Fill in all values
npm run dev
```

App runs at `http://localhost:3000`.
